MiniAgentOS M2 turns Goal > into a native goal shell.
This milestone proves the runtime can fetch a source, summarize it locally,
and return the summary directly to the user without any sink service.
